info:
  name: List Size Chart
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/size-charts/"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
