info:
  name: Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/auth/me"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
