info:
  name: show company
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/auth/agents/companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
