info:
  name: my performance
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/auth/agents/performance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
