info:
  name: agent leaderboard
  type: http
  seq: 14

http:
  method: GET
  url: "{{BASE_URL}}/admin/agents/leaderboard"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
