info:
  name: list users
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/admin/users"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
