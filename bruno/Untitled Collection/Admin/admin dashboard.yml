info:
  name: admin dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/admin/dashboard"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
