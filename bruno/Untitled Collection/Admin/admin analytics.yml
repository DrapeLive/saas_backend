info:
  name: admin analytics
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/admin/analytics"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
