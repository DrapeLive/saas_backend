info:
  name: List Customers
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/admin/customers/"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
