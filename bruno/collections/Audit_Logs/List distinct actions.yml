info:
  name: List distinct actions
  type: http
  seq: 3
  tags:
    - Audit_Logs

http:
  method: GET
  url: "{{baseUrl}}/api/audit-logs/actions/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/audit-logs/actions/"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            ""
          ]

docs: Returns all distinct action strings for the filter dropdown.
