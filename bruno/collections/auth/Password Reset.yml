info:
    name: Password Reset
    type: http
    seq: 7

http:
    method: POST
    url: "{{base_url}}/api/auth/password/reset"
    body:
        type: json
        data: |-
            {
              "email": "{{login_email}}"
            }
    auth:
        type: none

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });

settings:
    encodeUrl: true
