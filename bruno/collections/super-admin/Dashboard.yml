info:
    name: Dashboard
    type: http
    seq: 9

http:
    method: GET
    url: "{{base_url}}/api/super-admin/dashboard"
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });

settings:
    encodeUrl: true
