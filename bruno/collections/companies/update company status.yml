info:
  name: update company status
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/super-admin/companies/8728b69b-1e8c-44cc-a69b-51f1088789fd/status"
  body:
    type: json
    data: |-
      {
        "status": "active",
        "reason": "nothing reason"
      }
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
