info:
  name: retrieve company
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/super-admin/companies/39fdf6a9-46b8-4cb8-a56d-045e1e2ebbf0"
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
