info:
  name: Retrieve Plan
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/plans/e2318392-67ca-4d4f-93ef-6403c128d65d/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
