info:
  name: List Plan
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/plans/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
