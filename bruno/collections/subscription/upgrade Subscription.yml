info:
  name: upgrade Subscription
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/subscriptions/15c6292c-c43c-4f51-a2d4-463bdbafcf4b/upgrade/"
  body:
    type: json
    data: |-
      {
        "plan_id": "e2318392-67ca-4d4f-93ef-6403c128d65d",
        "billing_cycle": "monthly",
        "discount_pct": "10",
        "notes": "Good"
      }
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
