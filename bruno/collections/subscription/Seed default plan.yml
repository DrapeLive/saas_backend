info:
  name: Seed default plan
  type: http
  seq: 18

http:
  method: POST
  url: "{{baseUrl}}/api/plans/seed-defaults/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
