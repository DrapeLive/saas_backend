info:
  name: My Subscription
  type: http
  seq: 15

http:
  method: GET
  url: "{{BASE_URL}}/my-subscription/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
