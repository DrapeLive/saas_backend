info:
  name: List Subscription
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/subscriptions/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
