info:
  name: retrieve Subscription
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/subscriptions/15c6292c-c43c-4f51-a2d4-463bdbafcf4b/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
