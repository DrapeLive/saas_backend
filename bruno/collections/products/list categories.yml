info:
  name: list categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/categories/"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
