info:
  name: list categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/categories/"
  auth:
    type: bearer
    token: "{{AGENT_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
