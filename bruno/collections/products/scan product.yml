info:
  name: scan product
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/products/scan/df71d4c2-b74c-4e57-85b0-b119ef30cdd4/"
  headers:
    - name: X-Company-Id
      value: "{{COMPANY_ID}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{AGENT_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
