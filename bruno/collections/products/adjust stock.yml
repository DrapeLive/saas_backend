info:
  name: adjust stock
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseUrl}}/api/stock/movements/"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
