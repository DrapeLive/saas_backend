info:
  name: adjust stock
  type: http
  seq: 20

http:
  method: GET
  url: "{{BASE_URL}}/stock/movements/"
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
