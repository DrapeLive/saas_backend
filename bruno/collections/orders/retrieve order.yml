info:
  name: retrieve order
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/orders/85d90b34-9526-4c97-b277-2cb57b622f87/"
  headers:
    - name: X-Company-Id
      value: "{{COMPANY_ID}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
