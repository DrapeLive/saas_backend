info:
  name: retrieve order
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/orders/bbe4bacf-39ac-4cde-a60c-401ccdb820b2/"
  headers:
    - name: X-Company-Id
      value: "{{COMPANY_ID}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{AGENT_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
