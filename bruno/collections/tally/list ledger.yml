info:
  name: list ledger
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/tally/ledger-mappings/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
