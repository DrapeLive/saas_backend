info:
  name: sync health summary
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/tally/status/"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
