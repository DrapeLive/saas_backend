info:
  name: list tally logs
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/tally/logs/"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
