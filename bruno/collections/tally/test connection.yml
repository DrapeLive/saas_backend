info:
  name: test connection
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/tally/test-connection/"
  body:
    type: json
    data: |-
      {
        "tally_url"
      }
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
