info:
  name: Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/auth/me"
  auth:
    type: bearer
    token: "{{AGENT_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
