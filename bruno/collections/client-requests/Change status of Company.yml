info:
  name: Change status of Company
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/super-admin/companies/39fdf6a9-46b8-4cb8-a56d-045e1e2ebbf0/status"
  body:
    type: json
    data: |-
      {
        "status": "active",
        "reason": "nothing"
      }
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
