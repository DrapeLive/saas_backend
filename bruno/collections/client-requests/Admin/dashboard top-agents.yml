info:
  name: dashboard top-agents
  type: http
  seq: 30

http:
  method: GET
  url: "{{BASE_URL}}/admin/dashboard/top-agents"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
