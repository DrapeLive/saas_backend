info:
  name: show agents membership
  type: http
  seq: 35

http:
  method: GET
  url: "{{BASE_URL}}/admin/agents/overview"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
