info:
  name: customers overview
  type: http
  seq: 33

http:
  method: GET
  url: "{{BASE_URL}}/admin/customers/overview/"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
