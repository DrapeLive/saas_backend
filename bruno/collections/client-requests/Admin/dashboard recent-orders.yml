info:
  name: dashboard recent-orders
  type: http
  seq: 28

http:
  method: GET
  url: "{{BASE_URL}}/admin/dashboard/recent-orders"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
