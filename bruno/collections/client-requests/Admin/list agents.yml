info:
  name: list agents
  type: http
  seq: 13

http:
  method: GET
  url: "{{BASE_URL}}/admin/agents"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
