info:
  name: super admin dashboard
  type: http
  seq: 11

http:
  method: GET
  url: "{{BASE_URL}}/super-admin/dashboard"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
