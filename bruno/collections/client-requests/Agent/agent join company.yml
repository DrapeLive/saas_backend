info:
  name: agent join company
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/auth/agents/join"
  body:
    type: json
    data: |-
      {
         "invite_code": "" 
      }
  auth:
    type: bearer
    token: "{{access_agent}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
