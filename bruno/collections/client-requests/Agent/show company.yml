info:
  name: show company
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/auth/agents/companies"
  auth:
    type: bearer
    token: "{{AGENT_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
