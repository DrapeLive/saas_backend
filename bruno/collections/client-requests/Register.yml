info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth/signup"
  body:
    type: json
    data: |-
      {
        "email": "hamza@gmail.com",
        "password": "password!123",
        "full_name": "Hamza",
        "phone": "9878605748",
        "company_name":"KT Company",
        "company_slug": "kt-company"
      }
  auth:
    type: bearer
    token: "{{SUPERADMIN_ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
