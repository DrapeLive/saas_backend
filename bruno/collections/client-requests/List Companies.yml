info:
  name: List Companies
  type: http
  seq: 13

http:
  method: GET
  url: "{{BASE_URL}}/super-admin/companies"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
