info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/admin/users"
  body:
    type: json
    data: |-
      {
        "email": "subadmin@company.com",
        "password": "Sub@12345678",
        "full_name": "Sub Admin",
        "phone": "9876543213"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 201", function() {
          expect(res.status).to.equal(201);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
