info:
    name: Compute Segment
    type: http
    seq: 9

http:
    method: POST
    url: "{{base_url}}/api/admin/customers/{{customer_id}}/compute-segment"
    body:
        type: none
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });
              test("returns segment", function() {
                expect(res.body).to.have.property("segment");
              });

settings:
    encodeUrl: true
