info:
    name: Verify GSTIN
    type: http
    seq: 8

http:
    method: POST
    url: "{{base_url}}/api/admin/customers/{{customer_id}}/verify-gstin"
    body:
        type: none
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });
              test("returns valid field", function() {
                expect(res.body).to.have.property("valid");
              });

settings:
    encodeUrl: true
