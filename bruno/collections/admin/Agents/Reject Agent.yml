info:
    name: Reject Agent
    type: http
    folder: Agents
    seq: 6

http:
    method: POST
    url: "{{base_url}}/api/admin/agents/{{agent_membership_id}}/reject"
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    variables:
        - name: agent_membership_id
          value: ""
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });

settings:
    encodeUrl: true
