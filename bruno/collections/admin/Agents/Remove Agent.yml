info:
    name: Remove Agent
    type: http
    folder: Agents
    seq: 4

http:
    method: DELETE
    url: "{{base_url}}/api/admin/agents/{{agent_membership_id}}"
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    variables:
        - name: agent_membership_id
          value: ""
    scripts:
        - type: tests
          code: |-
              test("status is 204", function() {
                expect(res.status).to.equal(204);
              });

settings:
    encodeUrl: true
