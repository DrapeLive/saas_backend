info:
    name: Invitations List
    type: http
    seq: 12

http:
    method: GET
    url: "{{base_url}}/api/admin/invitations"
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });

settings:
    encodeUrl: true
