info:
    name: Setup Bank
    type: http
    seq: 6

http:
    method: PATCH
    url: "{{base_url}}/api/admin/setup/bank"
    body:
        type: json
        data: |-
            {
              "bank_name": "State Bank of India",
              "bank_account": "12345678901",
              "bank_ifsc": "SBIN0001234",
              "bank_branch": "Mumbai Main",
              "upi_id": "acmecorp@upi"
            }
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 200", function() {
                expect(res.status).to.equal(200);
              });

settings:
    encodeUrl: true
