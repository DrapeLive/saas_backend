info:
    name: Delete User
    type: http
    seq: 11

http:
    method: DELETE
    url: "{{base_url}}/api/admin/users/{{user_id}}"
    auth:
        type: bearer
        token: "{{auth_token}}"

runtime:
    scripts:
        - type: tests
          code: |-
              test("status is 204", function() {
                expect(res.status).to.equal(204);
              });

settings:
    encodeUrl: true
