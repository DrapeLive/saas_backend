info:
  name: Request password reset
  type: http
  seq: 8
  tags:
    - Authentication

http:
  method: POST
  url: "{{baseUrl}}/api/auth/password/reset"
  body:
    type: json
    data: |-
      {
        "email": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/auth/password/reset"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": "",
            "uid": "",
            "token": ""
          }
  - name: 400 Response
    description: Bad request. Field/validation errors are flattened into a single `detail` string.
    request:
      url: "{{baseUrl}}/api/auth/password/reset"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": ""
          }
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": ""
          }

docs: Starts the password reset flow. Returns the `uid` and `token` required by the confirm endpoint.
