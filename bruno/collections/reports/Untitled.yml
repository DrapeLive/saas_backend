info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/reports/sales/"
  auth:
    type: bearer
    token: "{{ACCESS}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
