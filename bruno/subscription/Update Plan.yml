info:
  name: Update Plan
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/plans/6847d0ae-368a-4ca9-94c1-2b3e64a7b504/"
  body:
    type: json
    data: |-
      {
          "name": "Starter Plan"
      }
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
