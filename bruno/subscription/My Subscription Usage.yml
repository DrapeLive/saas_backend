info:
  name: My Subscription Usage
  type: http
  seq: 16

http:
  method: GET
  url: "{{BASE_URL}}/my-subscription/usage/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{access_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
