info:
  name: List Plan
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/plans/"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
